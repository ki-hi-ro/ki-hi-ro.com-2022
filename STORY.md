# Storyの運用

通常の投稿とは別の固定ページとして管理します。カスタム投稿タイプや追加プラグインは不要です。

## 初回セットアップ

テーマのファイルを反映した後、WordPress管理画面の **ツール → Storyのセットアップ** で「初期ページを作成」を実行します。

- 親ページ：`/story/`、テンプレート「Story — 一覧」
- 子ページ：`/story/100-days/`、テンプレート「Story — 本編」

両方とも下書きで作成されます。本文を確認して親・子の両方を公開してください。親ページの公開後、ヘッダーにStoryへのリンクが表示されます。テーマのファイルを送るだけでは固定ページのデータは移りません。

セットアップは既存のページや編集内容を上書きしません。同じURLに別のテンプレートのページがあれば、エラーを表示して停止します。初期本文は `content/story-100-days.php` にありますが、作成後の編集はWordPress側で行ってください。

ローカル検証では2ページを作成・公開済みです。本番のデータベースは変更していません。

## 本文・期間・関連記事

- 本文：ブロックエディターで編集します。7章と締めくくりは独立したグループです。
- 期間：固定ページ編集画面の「Storyの掲載期間」で設定します。
- 一覧の紹介文：固定ページの「抜粋」で設定します。
- 出典記事：初期Storyは公開記事29件を参照し、各章末のHTMLブロックに公開日・記事名・本番URLを保存しています。引用は引用ブロックで編集できます。ローカルに存在しない記事も本番URLから読めます。
- 追加の関連記事：ショートコードブロックを `[story_related_posts ids="123,456"]` のように編集します。指定順に最大5件の公開済みBlog投稿を表示します。空欄なら表示しません。リンク先の記事は自動選定していません。
- 章ナビゲーション：HTMLブロックのリンク先と、各グループのHTMLアンカーを合わせます。

100日Storyは2026年6月12日〜9月19日の公開記事に基づき、日付別の出来事を7章にまとめています。公開日と出来事の日付を区別し、7つの短い原文引用と振り返りの文章を分けています。初期データを変更しても既存ページは自動更新されません。今回の改稿はローカルの本文・抜粋にも反映済みです。

## 次のStoryを追加

1. 新しい固定ページを作成します。
2. 親ページをStory、テンプレートを「Story — 本編」にします。
3. タイトル・スラッグ・抜粋・期間・本文を入力して公開します。
4. 一覧には公開した直接の子ページが自動で表示されます。「順序」、同値ならタイトル順です。

最初のStoryのブロックを複製して編集すると同じ章レイアウトを使えます。専用CSSクラスは `story-intro`、`story-chapter`、`story-chapter__meta`、`story-ending` です。新しいStoryの本文をPHPに追加する必要はありません。

## SEOと表示

titleとcanonicalはWordPress標準の出力を利用します。StoryのみdescriptionとOGPを補完します。アイキャッチを設定した場合はOGP画像にも使います。

Yoast、Rank Math、AIOSEO、SEOPress、The SEO Framework、Slim SEOを検出した場合、テーマのメタタグ出力は停止します。別のSEO実装を導入する場合は `kihiro_story_has_seo_plugin` フィルターで `true` を返してください。

Story用CSSは専用テンプレートにだけ読み込まれます。Easy Table of Contentsの自動挿入はStoryでは停止し、章ナビゲーションを使います。アニメーションや外部ライブラリは追加していません。
